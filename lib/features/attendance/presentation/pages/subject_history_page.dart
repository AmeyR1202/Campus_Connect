import 'package:flutter/material.dart';

class SubjectHistoryPage extends StatelessWidget {
  const SubjectHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
